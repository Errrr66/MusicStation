<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.music.mapper.PlaylistMapper">

    <!-- 定义 ResultMap -->
    <resultMap id="PlaylistDetailVOResultMap" type="com.example.music.model.vo.PlaylistDetailVO">
        <id column="playlistId" property="playlistId"/>
        <result column="title" property="title"/>
        <result column="coverUrl" property="coverUrl"/>
        <result column="introduction" property="introduction"/>
        <!-- 歌曲改为独立子查询，避免与评论 JOIN 产生笛卡尔积 -->
        <collection property="songs" ofType="com.example.music.model.vo.SongVO"
                    select="com.example.music.mapper.PlaylistMapper.listPlaylistSongs"
                    column="playlistId" fetchType="lazy"/>
        <!-- 评论改为独立子查询，避免与歌曲 JOIN 产生笛卡尔积 -->
        <collection property="comments" ofType="com.example.music.model.vo.CommentVO"
                    select="com.example.music.mapper.CommentMapper.listPlaylistComments"
                    column="playlistId" fetchType="lazy"/>
    </resultMap>

    <!-- 查询歌单详情 -->
    <select id="getPlaylistDetailById" resultMap="PlaylistDetailVOResultMap">
        SELECT p.id           AS playlistId,
               p.title        AS title,
               p.cover_url    AS coverUrl,
               p.introduction AS introduction
        FROM tb_playlist p
        WHERE p.id = #{playlistId}
    </select>

    <!-- 查询歌单下的歌曲列表（独立查询） -->
    <select id="listPlaylistSongs" resultType="com.example.music.model.vo.SongVO">
        SELECT s.id           AS songId,
               s.name         AS songName,
               s.album        AS album,
               s.duration     AS duration,
               s.cover_url    AS coverUrl,
               s.audio_url    AS audioUrl,
               s.release_time AS releaseTime,
               a.name         AS artistName
        FROM tb_playlist_binding pb
                 LEFT JOIN tb_song s ON pb.song_id = s.id
                 LEFT JOIN tb_artist a ON s.artist_id = a.id
        WHERE pb.playlist_id = #{playlistId}
        ORDER BY s.id DESC
    </select>

    <!-- 获取用户收藏歌单的风格 -->
    <select id="getFavoritePlaylistStyles" resultType="java.lang.String">
        SELECT style
        FROM tb_playlist
        WHERE id IN
        <foreach item="id" collection="favoritePlaylistIds" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 根据风格推荐歌单（排除已收藏歌单） -->
    <select id="getRecommendedPlaylistsByStyles" resultType="com.example.music.model.vo.PlaylistVO">
        SELECT *
        FROM tb_playlist
        WHERE style IN
        <foreach item="style" collection="sortedStyleIds" open="(" separator="," close=")">
            #{style}
        </foreach>
        AND id NOT IN
        <foreach item="id" collection="favoritePlaylistIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <!-- 根据歌单id列表查询歌单列表 -->
    <select id="getPlaylistsByIds" resultType="com.example.music.model.vo.PlaylistVO">
        SELECT
        p.id AS playlistId,
        p.title AS title,
        p.cover_url AS coverUrl
        FROM tb_playlist p
        LEFT JOIN tb_user_favorite u ON p.id = u.playlist_id AND u.user_id = #{userId}
        WHERE
        <if test="playlistIds != null and playlistIds.size() > 0">
            p.id IN
            <foreach collection="playlistIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        <if test="title != null and title.trim() != ''">
            AND p.title LIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="style != null and style.trim() != ''">
            AND p.style LIKE CONCAT('%', #{style}, '%')
        </if>
        ORDER BY u.create_time DESC
    </select>

    <!-- Agent RAG: 按关键词搜索歌单 -->
    <select id="searchPlaylistsByKeyword" resultType="com.example.music.model.vo.PlaylistVO">
        SELECT p.id AS playlistId,
               p.title AS title,
               p.cover_url AS coverUrl
        FROM tb_playlist p
        WHERE p.title LIKE CONCAT('%', #{keyword}, '%')
           OR p.style LIKE CONCAT('%', #{keyword}, '%')
           OR p.introduction LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY p.id DESC
        LIMIT #{limit}
    </select>

</mapper>